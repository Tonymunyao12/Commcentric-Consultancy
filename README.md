 # Commcentric Consultancy

The Commcentric Consultancy website is a dependency-free static site built from the visual identity and contact details on the supplied business cards.

## Run locally

Open `index.html` in a browser, or serve the folder with any static HTTP server:

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.
